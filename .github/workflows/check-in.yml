name: V2Free 每日签到

on:
  schedule:
    # 每天北京时间 00:30 执行，可根据需要调整
    - cron: '30 16 * * *'
  workflow_dispatch:

jobs:
  checkin:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: |
          pip install playwright requests
          playwright install chromium

      - name: 执行签到
        env:
          V2FREE_USERNAME: ${{ secrets.V2FREE_USERNAME }}
          V2FREE_PASSWORD: ${{ secrets.V2FREE_PASSWORD }}
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
        run: python check_in.py

      # 如果失败，上传截图用于调试
      - name: 上传错误截图
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: error-screenshots
          path: "*.png"
